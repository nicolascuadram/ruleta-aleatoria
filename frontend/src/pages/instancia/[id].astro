---
import Layout from "../../layouts/Layout.astro";
import Instancia from "../../components/instancias/Instancia.vue";

// Obtener el parámetro id de la URL
const { id } = Astro.params as { id: string };
---

<Layout>
	<Instancia id={id} client:load />
</Layout>