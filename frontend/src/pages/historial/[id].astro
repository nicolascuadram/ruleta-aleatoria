---
import Layout from "../../layouts/Layout.astro";
import Historial from "../../components/historial/Historial2.vue";

// Obtener el parámetro id de la URL
const { id } = Astro.params as { id: string };
---

<Layout>
	<Historial id={id} client:load />
</Layout>